import * as objectHelpers from "./object/objectHelpers";
import * as stringHelpers from "./string/stringHelpers";
export namespace Helpers {
  export const Object = objectHelpers;
  export const String = stringHelpers;
}
