import { Token as TokenControllers } from "./token";
import { ItemControllers } from "./items";

export namespace Controllers {
  export const Token = TokenControllers;
  export const Items = ItemControllers;
}
