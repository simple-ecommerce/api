import { itemSchemas } from "./item_schemas";
import { specificationSchemas } from "./specification_schemas";

export namespace ValidationSchemas {
  export const Item = itemSchemas;
  export const Specification = specificationSchemas;
}
